// chat controller — placeholder
// Business logic to be implemented in Phase 1-2

const sendMessage = (req, res) => {
  res.json({ message: 'Send message - not yet implemented' });
};

const getMessages = (req, res) => {
  res.json({ message: 'Get messages - not yet implemented' });
};

module.exports = {
  sendMessage,
  getMessages,
};
