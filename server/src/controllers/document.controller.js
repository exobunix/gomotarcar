// document controller — placeholder
// Business logic to be implemented in Phase 1-2

const getDocuments = (req, res) => {
  res.json({ message: 'Get documents - not yet implemented' });
};

const uploadDocument = (req, res) => {
  res.json({ message: 'Upload document - not yet implemented' });
};

module.exports = {
  getDocuments,
  uploadDocument,
};
