// offer controller — placeholder
// Business logic to be implemented in Phase 1-2

const getOffers = (req, res) => {
  res.json({ message: 'Get offers - not yet implemented' });
};

const createOffer = (req, res) => {
  res.json({ message: 'Create offer - not yet implemented' });
};

const updateOffer = (req, res) => {
  res.json({ message: 'Update offer - not yet implemented' });
};

module.exports = {
  getOffers,
  createOffer,
  updateOffer,
};
