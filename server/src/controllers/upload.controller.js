// upload controller — placeholder
// Business logic to be implemented in Phase 1-2

const uploadFile = (req, res) => {
  res.json({ message: 'Upload file - not yet implemented' });
};

const getUploadedFile = (req, res) => {
  res.json({ message: 'Get uploaded file - not yet implemented' });
};

module.exports = {
  uploadFile,
  getUploadedFile,
};
